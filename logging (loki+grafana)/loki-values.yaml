deploymentMode: SingleBinary

loki:
  auth_enabled: false
  common:
    replication_factor: 1
    path_prefix: /var/loki
    storage:
      s3:
        endpoint: http://minio.minio.svc.cluster.local:9000
        bucketnames: loki-data
        access_key_id: loki
        secret_access_key: FeerDe3o # 👈 ПРОВЕРЬТЕ ПАРОЛЬ
        s3forcepathstyle: true
        insecure: true

  schemaConfig:
    configs:
      - from: 2024-04-01
        store: tsdb
        object_store: s3
        schema: v13
        index:
          prefix: index_
          period: 24h
  
  # Настройки хранения
  storage:
    type: s3
    bucketNames:
      chunks: loki-data
      ruler: loki-data
      admin: loki-data

  compactor:
    working_directory: /var/loki/compactor
    #shared_store: s3
    delete_request_store: s3
    compaction_interval: 10m
    retention_enabled: true
    retention_delete_delay: 2h
    retention_delete_worker_count: 150
    
  limits_config:
    retention_period: 30d

singleBinary:
  replicas: 1
  persistence:
    enabled: true
    storageClass: managed-nfs-storage
    size: 20Gi 
  podSecurityContext:
    fsGroup: 65534
    runAsUser: 65534
    runAsGroup: 65534

# 2. Отключаем Gateway (он нам не нужен внутри кластера, будем ходить напрямую)
gateway:
  enabled: false

# 3. Отключаем Memcached (экономим RAM, для одной ноды Loki он избыточен)
chunksCache:
  enabled: false
resultsCache:
  enabled: false

# 4. Явно отключаем микросервисные компоненты
backend:
  replicas: 0
read:
  replicas: 0
write:
  replicas: 0
ingester:
  replicas: 0
querier:
  replicas: 0

# Отключаем встроенный мониторинг, если у вас уже есть свой prometheus-stack
monitoring:
  selfMonitoring:
    enabled: false
    lokiCanary:
      enabled: false
  serviceMonitor:
    enabled: true
    metricsInstance:
      enabled: false # Не создавать новый инстанс Prometheus
    namespace: monitoring
    labels:
      release: kube-prometheus-stack
    interval: 15s
    scrapeTimeout: 10s
  prometheusRule:
    enabled: true
    labels:
      release: prometheus
    rules:
      - alert: LokiRequestErrors
        expr: 100 * sum(rate(loki_request_duration_seconds_count{status_code=~"5.."}[1m])) / sum(rate(loki_request_duration_seconds_count[1m])) > 10
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "Loki is returning > 10% 5xx errors"      