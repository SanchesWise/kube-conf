apiVersion: apps/v1
kind: Deployment
metadata:
  name: deepseek-r1
  namespace: ai-agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deepseek-r1
  template:
    metadata:
      labels:
        app: deepseek-r1
    spec:
      # 1. Ваш кастомный RuntimeClass
      runtimeClassName: nvidia
      
      # Использовать точное имя Label из вашего вывода
      nodeSelector:
        gpu-performance: "high"
      
      # Использовать точное имя Taint из вашего вывода
      tolerations:
      - key: "gpu"
        operator: "Equal"
        value: "high-performance"
        effect: "NoSchedule"

      containers:
      - name: vllm-engine
        image: vllm/vllm-openai:latest
        resources:
          limits:
            nvidia.com/gpu: 2
        env:
        - name: NVIDIA_VISIBLE_DEVICES
          value: "all"
        # Путь к нашему быстрому NVMe
        - name: HF_HOME
          value: "/model-cache"
        - name: VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS
          value: "1"
        args:
        - "--model"
        - "casperhansen/deepseek-r1-distill-llama-70b-awq"
        - "--quantization"
        - "awq"
        - "--dtype"
        - "float16"
        - "--tensor-parallel-size"
        - "2"
        - "--max-model-len"
        - "16384" 
        # СНИЖАЕМ ДО 0.90, чтобы оставить 2.4 ГБ "воздуха" на каждой карте
        - "--gpu-memory-utilization"
        - "0.9328"
        # ОГРАНИЧИВАЕМ ПАРАЛЛЕЛИЗМ (для агента 32 задачи разом не нужно)
        - "--max-num-seqs"
        - "32"
        - "--trust-remote-code"
        - "--enable-prefix-caching"
        ports:
        - containerPort: 8000
        volumeMounts:
        - name: model-cache
          mountPath: /model-cache
        # Подключаем ваши логи, как вы сделали ранее
        - name: log-volume
          mountPath: /var/log
      
      volumes:
      - name: model-cache
        hostPath:
          path: /mnt/nvme-storage
          type: DirectoryOrCreate
      - name: log-volume
        hostPath:
          path: /var/log
          type: Directory       