# ============================================================================
# Nexus 3 с разделением хранилищ + Self-Signed TLS сертификат
# БД (OrientDB) → Longhorn | Блобы → MinIO S3
# ============================================================================

---
# 1. Создаём Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: nexus


---
# 2a. PVC для БД (Longhorn, SSD)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-db
  namespace: nexus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi


---
# 2b. Small PVC для конфигов и логов
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexus-config
  namespace: nexus
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi


---
# 3. Certificate для Nexus (self-signed via ccsfarm-ca-issuer)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nexus-tls
  namespace: nexus
spec:
  secretName: nexus-tls-secret
  duration: 8760h # 1 год
  renewBefore: 720h # 30 дней до истечения
  commonName: nexus.ccsfarm.local
  dnsNames:
    - nexus.ccsfarm.local
    - registry-nexus.ccsfarm.local
  issuerRef:
    name: ccsfarm-ca-issuer
    kind: ClusterIssuer


---
# 4. Основное приложение (Deployment)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus
  namespace: nexus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nexus
  template:
    metadata:
      labels:
        app: nexus
    spec:
      initContainers:
        - name: fix-permissions
          image: busybox:1.36
          command: 
            - sh
            - -c
            - >
              mkdir -p /nexus-data/blobs &&
              mkdir -p /nexus-data/db &&
              mkdir -p /nexus-data/log &&
              mkdir -p /nexus-data/tmp &&
              mkdir -p /nexus-data/etc &&
              mkdir -p /nexus-config &&
              chown -R 200:200 /nexus-data /nexus-config &&
              chmod -R 755 /nexus-data /nexus-config
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: nexus-db
              mountPath: /nexus-data
            - name: nexus-config
              mountPath: /nexus-config
      containers:
        - name: nexus
          image: sonatype/nexus3:3.70.1
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 200
            runAsGroup: 200
          ports:
            - containerPort: 8081
              name: ui
            - containerPort: 5000     # <--- ИСПРАВЛЕНО: Теперь официально 5000
              name: docker-hosted
          env:
            - name: NEXUS_CONTEXT
              value: "/"
            - name: INSTALL4J_ADD_VM_PARAMS
              value: "-Xms1g -Xmx2g -XX:+UseG1GC"
          volumeMounts:
            - name: nexus-db
              mountPath: /nexus-data
            - name: nexus-config
              mountPath: /nexus-config
          livenessProbe:
            httpGet:
              path: /service/rest/v1/status
              port: 8081
            initialDelaySeconds: 120
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /service/rest/v1/status
              port: 8081
            initialDelaySeconds: 120
            periodSeconds: 30
      volumes:
        - name: nexus-db
          persistentVolumeClaim:
            claimName: nexus-db
        - name: nexus-config
          persistentVolumeClaim:
            claimName: nexus-config
---
apiVersion: v1
kind: Service
metadata:
  name: nexus-service
  namespace: nexus
spec:
  selector:
    app: nexus
  type: ClusterIP
  ports:
    - name: ui
      port: 8081
      targetPort: 8081
      protocol: TCP
    - name: docker
      port: 8082           # Внешний порт (для Ingress)
      targetPort: 5000     # Внутренний порт (в Pod)
      protocol: TCP

---
# 6. Ingress (Маршрутизация с Self-Signed TLS)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nexus-ingress
  namespace: nexus
  annotations:
    # cert-manager annotations для self-signed
    cert-manager.io/issuer-group: cert-manager.io
    cert-manager.io/issuer-kind: ClusterIssuer
    cert-manager.io/issuer-name: ccsfarm-ca-issuer
    # Критически важные настройки для Docker Registry
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "900"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "900"
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    # Отключить request body checking (для больших образов)
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-headers: "Content-Type, Authorization"
    # Перенаправлять HTTP на HTTPS
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"    
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - registry-nexus.ccsfarm.local
        - nexus.ccsfarm.local
      secretName: nexus-tls-secret  # ✅ TLS Secret из Certificate (self-signed)
  rules:
    # Хост для UI админки
    - host: nexus.ccsfarm.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus-service
                port:
                  number: 8081
    # Хост для команд docker push/pull
    - host: registry-nexus.ccsfarm.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus-service
                port:
                  number: 8082
